// var queries = require('./queries');
// queries.generate();
// var test = queries.getSql('select-shifts');
// console.log(test);
function greeter(person) {
    return "Hello " + person + "!";
}
var namesd = 'Node Hero';
console.log(greeter(namesd));
